[defaults]
inventory = inventory/hosts.ini
roles_path = roles
host_key_checking = False     # not recommended for production
retry_files_enabled = False   # don't drop *.retry file at failure
stdout_callback = yaml

[ssh_connection]
pipelining = True             # runs each task in a single SSH session- faster
